const {chunk, flatten, flattenDeep, uniq, uniqBy, groupBy, orderBy, random, get, has, set, unset, invert, merge,
omit, omitBy, pick, pickBy, capitalize, upperFirst, startCase, split, range, toPath} = require('./lodash.custom')

module.exports = {
  // Array
  chunk,
  flatten,
  flattenDeep,
  uniq,
  uniqBy,
  groupBy,
  orderBy,
  range,
  // Number
  random,
  // Object
  get,
  has,
  set,
  unset,
  invert,
  merge,
  omit,
  omitBy,
  pick,
  pickBy,
  // String
  capitalize,
  upperFirst,
  startCase,
  split,
  // Util
  toPath,
}
